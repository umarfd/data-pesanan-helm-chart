apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pinus-landing.fullname" . }}-test-connection"
  labels:
    {{- include "pinus-landing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pinus-landing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
