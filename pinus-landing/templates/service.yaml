apiVersion: v1
kind: Service
metadata:
  name: {{ include "pinus-landing.fullname" . }}
  labels:
    {{- include "pinus-landing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pinus-landing.selectorLabels" . | nindent 4 }}
